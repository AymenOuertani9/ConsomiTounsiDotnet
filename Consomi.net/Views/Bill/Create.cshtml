@model Consomi.net.Models.Bill

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(model => model.IdBill)
    @Html.HiddenFor(model => model.NumBill)
    @Html.HiddenFor(model => model.Totalfinal)
    @Html.HiddenFor(model => model.Datereglement)
    @Html.HiddenFor(model => model.Datebill)
   
    <div class="form-horizontal">
        <h4>Bill</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })






       

        <div class="form-group">
            @Html.LabelFor(model => model.Idlig, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <h3>Code_Order</h3>
                @Html.DropDownList("Idlig", null, htmlAttributes: new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.Idlig, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
